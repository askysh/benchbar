#@version 1
# __HEADER__
# Generated by frappe-mac: a lean Procfile with no watch and no schedule.
# "bench setup procfile" and "bench update" rewrite Procfile, never this file.
# Run "frappe-mac watch" (benchwatch) for asset rebuilds.
redis_cache: redis-server config/redis_cache.conf
redis_queue: redis-server config/redis_queue.conf
web: bench serve --port __WEB_PORT__
socketio: bench socketio
worker: bench worker 1>> logs/worker.log 2>> logs/worker.error.log
