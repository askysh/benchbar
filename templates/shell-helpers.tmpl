#@version 1
# __HEADER__
# Managed by benchbar (https://github.com/askysh/frappe-mac-dev-server).
# Do not edit inside the markers: run "benchbar repair" to regenerate.
__PROFILE_EXPORTS__
case ":$PATH:" in *":$HOME/.local/bin:"*) ;; *) export PATH="$HOME/.local/bin:$PATH" ;; esac
BENCHBAR="__BENCHBAR__"
benchup()      { "$BENCHBAR" up "$@"; }
benchdown()    { "$BENCHBAR" down "$@"; }
benchrestart() { "$BENCHBAR" restart "$@"; }
benchstatus()  { "$BENCHBAR" status "$@"; }
benchlogs()    { "$BENCHBAR" logs "$@"; }
benchfg()      { "$BENCHBAR" fg "$@"; }
benchwatch()   { "$BENCHBAR" watch "$@"; }
benchdoctor()  { "$BENCHBAR" doctor "$@"; }
benchcd()      { cd "$("$BENCHBAR" path)" || return 1; }
